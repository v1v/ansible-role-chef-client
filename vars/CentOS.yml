---
chef_client_package:
  platform: el
  arch: "{{ ansible_architecture }}"
  installer_dest: "{{ chef_client_installer_dir }}/{{ chef_client_version }}.rpm"
  platform_version: "6"
  #platform_version: "{{ ansible_lsb.major_release }}"
